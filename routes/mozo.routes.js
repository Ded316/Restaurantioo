/**
 * 
 * GET: orden
 * POST: orden (uno o mas platos, asociada a una mesa en particular)
 * 
 */

const express = require('express')
const app = express()

//Importar los modelos de datos que sean necesarios y definir las rutas correspondientes


module.exports = app;